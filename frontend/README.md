# Scanner Frontend

React + Vite UI for the NSE scanner backend.

## Setup

```bash
cd frontend
npm install
npm run dev
```

Opens on http://localhost:5173 and proxies `/api/*` to the backend at
http://localhost:4000 (start the backend first: `cd ../backend && npm run dev`).
